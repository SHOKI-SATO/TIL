# UPDATE文

## ▼基本型
#### ●UPDATE文
```sql
--レコードを更新
UPDATE tables SET column = 'data' WHERE user_id = 1;
```
<br>

## ▼その他
#### ●カラムの値を増減させる
>・現在のデータを基準にして指定
```sql
UPDATE tables SET price = price + 100 WHERE user_id = 1;
--

```

#### ●句
>・
```sql
--

```

#### ●句
>・
```sql
--

```

